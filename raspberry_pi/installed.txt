installed:

python (3.9 to match local conda)
python3-pip
pyserial (installed via pip)
usbmount (custom files)
debhelper 
build-essential
Radetich/usbmount (installer inside of ~/programs/usbmount)

to install:
SystemD Script to recieve UART



NOTES:

Run sudo raspi-config Interfaces -> Serial and answer "no", "yes" to the next two prompts.
Sudo vi /boot/config.txt and add dtoverlay=disable-bt. This is not strictly necessary.